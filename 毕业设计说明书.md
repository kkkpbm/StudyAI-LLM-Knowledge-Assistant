# 智能知识管理助手的设计与实现

## 摘要

随着互联网信息量的爆炸式增长，个人知识管理变得越来越重要。传统的笔记工具仅提供简单的文本存储和分类功能，缺乏智能化的知识组织与检索能力。本文设计并实现了一个基于大语言模型的智能知识管理助手系统。该系统采用前后端分离架构，前端基于 Vue 3 + TypeScript 构建用户界面，后端采用 Spring Boot 提供业务 API，并结合 FastAPI + LangChain + DeepSeek 构建 AI Agent 层，提供 RAG 智能问答、知识图谱提取、学习计划生成、间隔重复复习、智能标签推荐、学情诊断等智能功能。系统使用 ChromaDB 向量数据库存储笔记嵌入和聊天记忆，实现语义级的知识检索。在交互体验方面，实现了流式 SSE 输出、requestAnimationFrame 批量渲染、思考过程可视化、停止生成与重新生成等机制。实际使用表明，该系统能够有效帮助用户管理知识、提升学习效率。

**关键词**：知识管理；RAG；大语言模型；ChromaDB；间隔重复；SSE 流式输出；短期记忆

---

## 1. 绪论

### 1.1 研究背景

在当今信息爆炸的时代，人们每天需要处理大量的文字信息。学生在学习过程中需要记录课堂笔记、整理知识体系；职场人士需要不断学习新技能并构建自己的知识框架。然而，传统的笔记工具（如 OneNote、Notion、印象笔记等）虽然提供了基本的文本编辑和分类功能，却存在以下不足：

1. **被动存储**：笔记仅作为静态文本存储，缺乏主动的知识组织与关联能力
2. **检索效率低**：关键词搜索无法理解语义，难以找到真正相关的内容
3. **缺乏个性化学习支持**：无法根据用户的学习进度提供复习提醒或学习路径规划
4. **知识孤岛**：笔记之间缺乏关联，难以形成系统化的知识体系

近年来，以 ChatGPT 为代表的大语言模型（LLM）取得了巨大突破，为智能化知识管理带来了新的可能性。借助 LLM 的自然语言理解和生成能力，可以实现语义级的知识检索、自动摘要、知识图谱构建等高级功能。

### 1.2 研究意义

本课题旨在设计并实现一个智能知识管理助手，将大语言模型与传统笔记管理相结合，具有以下意义：

1. **提高学习效率**：通过 RAG 技术让用户用自然语言提问即可从笔记库中获取答案
2. **辅助知识体系构建**：自动提取笔记中的概念及其关系，形成可视化知识图谱
3. **科学复习规划**：基于 SM-2 间隔重复算法安排复习计划，对抗遗忘曲线
4. **个性化学习路径**：根据用户目标自动生成学习计划，引导系统性学习
5. **智能化知识沉淀**：AI 回答可一键保存为笔记，形成知识正向积累

### 1.3 国内外研究现状

**知识管理工具**方面，国外有 Notion、Roam Research、Obsidian 等工具，其中 Roam Research 和 Obsidian 引入了双向链接的概念，帮助用户建立笔记间的关联。国内有印象笔记、有道云笔记、飞书文档等，主要提供基础的笔记存储和分类功能。

**AI 辅助学习**方面，Anki 等工具基于间隔重复算法帮助用户记忆，但需要手动创建卡片。近年来，ChatGPT 等 LLM 被广泛应用于辅助学习，但直接使用 LLM 存在知识滞后和幻觉问题。

**RAG 技术**方面，LangChain、LlamaIndex 等框架提供了成熟的 RAG 实现方案，使得将 LLM 与私有知识库结合成为可能。ChromaDB、Pinecone 等向量数据库为语义检索提供了高效的存储和查询能力。

---

## 2. 系统需求分析

### 2.1 功能需求

系统主要包括以下功能模块：

#### 2.1.1 笔记管理
- 支持 Markdown 格式笔记的创建、编辑、删除
- 笔记分类与标签管理
- 笔记搜索（关键词搜索 + 语义搜索）
- AI 智能标签推荐

#### 2.1.2 智能问答
- 基于用户笔记库的 RAG 问答
- 支持针对特定笔记提问
- 流式输出（SSE），requestAnimationFrame 批量渲染
- 短期记忆：将当前会话最近对话历史注入 Prompt
- 长期记忆：聊天记录向量化存储与语义检索
- 追问引导：模糊问题时先反问澄清
- 停止生成与重新生成
- 回答一键保存为笔记

#### 2.1.3 知识图谱
- 自动从笔记内容中提取知识概念及其关系
- 知识图谱可视化展示
- 知识点搜索与定位

#### 2.1.4 学习计划
- AI 根据学习目标自动生成学习路径
- 学习任务管理（完成任务标记）
- 学习进度跟踪

#### 2.1.5 复习管理
- 基于 SM-2 间隔重复算法的复习提醒
- 创建笔记时自动生成复习提醒
- 学习打卡与学习时长记录

#### 2.1.6 学习仪表盘
- 学习数据概览（笔记数、学习时长、知识节点数等）
- 学习趋势图表
- 知识分布图表
- AI 学情诊断：实时分析学习数据，给出个性化建议

#### 2.1.7 辅助智能功能
- 文本自动摘要
- 学习材料难度评估
- 智能标签/分类推荐

### 2.2 非功能需求

- **响应时间**：普通 API 响应时间 < 2s，流式问答首 token 延迟 < 3s
- **渲染流畅性**：前端采用 requestAnimationFrame 批量渲染，保持 60fps 更新频率
- **安全性**：用户认证采用 JWT Token，接口鉴权
- **可扩展性**：AI Agent 独立部署，可独立扩展
- **易用性**：界面简洁清晰，交互流畅

---

## 3. 系统设计

### 3.1 系统架构

系统采用前后端分离的三层架构，前端通过 Spring Boot 代理访问 AI Agent：

```
┌──────────────────────────────────────────┐
│          前端展示层（Vue 3 + TS）          │
│   Element Plus + ECharts + Vditor         │
└─────────────┬────────────────┬────────────┘
              │                │
      HTTP/REST          HTTP/REST
              ▼                ▼
┌──────────────────┐  ┌─────────────────────┐
│  业务服务层       │  │  AI Agent 服务层     │
│  (Spring Boot)   │  │  (FastAPI +         │
│  ─────────────── │  │   LangChain)        │
│  · 用户认证 JWT  │  │  ───────────────    │
│  · 笔记 CRUD     │  │  · RAG 问答         │
│  · 分类/标签管理 │  │  · 知识图谱         │
│  · 学习计划管理  │  │  · 学习计划生成     │
│  · 复习/打卡记录 │  │  · 文本摘要         │
│  · 数据统计      │  │  · 语义搜索         │
│  · AI 代理转发   │  │  · 聊天记忆         │
│                  │  │  · 标签推荐         │
│  ← MySQL ────────│  │  · 学情诊断         │
└──────┬───────────┘  └──────────┬──────────┘
       │                         │
       ▼                         ▼
┌──────────────┐      ┌──────────────────┐
│   MySQL      │      │   ChromaDB       │
│  (业务数据)   │      │  (向量嵌入存储)   │
│              │      │  + Sentence      │
│              │      │  Transformers    │
└──────────────┘      └──────────────────┘
```

**各层职责：**

- **前端层**：Vue 3 + TypeScript + Composition API，Element Plus 组件库，Pinia 状态管理，Vue Router 路由管理。负责 UI 交互、SSE 流式接收、Markdown 实时渲染、思考过程可视化
- **业务 API 层**：Spring Boot + MyBatis-Plus，处理用户认证、笔记 CRUD、分类标签等业务逻辑，同时作为 AI Agent 的代理层转发 AI 请求
- **AI Agent 层**：FastAPI + LangChain，调用 DeepSeek API，集成 ChromaDB 向量数据库。处理 RAG 检索增强生成、知识图谱提取、学习计划生成、文本摘要、难度评估、标签推荐、学情诊断等 AI 任务
- **数据层**：MySQL 存储结构化业务数据，ChromaDB 存储向量嵌入数据（笔记 + 聊天记忆）

### 3.2 技术选型

| 技术组件 | 选型 | 说明 |
|---------|------|------|
| 前端框架 | Vue 3 + TypeScript | 渐进式框架，组合式 API 便于逻辑复用 |
| 构建工具 | Vite | 快速的开发服务器和构建工具 |
| UI 组件库 | Element Plus | 成熟的中文 UI 组件库 |
| 状态管理 | Pinia | Vue 3 官方推荐的状态管理 |
| Markdown 编辑器 | Vditor | 支持所见即所得的 Markdown 编辑器 |
| 图表库 | ECharts | 强大的可视化图表库 |
| 后端框架 | Spring Boot | 成熟的 Java 企业级开发框架 |
| ORM | MyBatis-Plus | 简化数据库操作，Lambda 查询 |
| 服务间通信 | Spring WebClient (Reactor) | 响应式 HTTP 客户端，支持 SSE 流代理 |
| AI 框架 | FastAPI + LangChain | 高性能异步框架 + LLM 应用框架 |
| LLM | DeepSeek API | 高性价比的中文大语言模型 |
| 向量数据库 | ChromaDB | 轻量级、本地化的向量数据库 |
| 嵌入模型 | Sentence Transformers | 多语言语义嵌入模型（paraphrase-multilingual-MiniLM-L12-v2） |
| 关系数据库 | MySQL | 稳定的关系型数据库 |

### 3.3 模块设计

#### 3.3.1 笔记管理模块
- 支持 Markdown 格式的富文本编辑
- 笔记关联分类和标签，支持多标签
- 笔记的向量嵌入同步至 ChromaDB
- 创建笔记时自动生成复习提醒

#### 3.3.2 RAG 问答模块
- 用户提问时，先从 ChromaDB 并行检索相关笔记片段和聊天记忆
- 将当前会话最近对话历史（短期记忆）注入 Prompt
- 将检索结果和对话历史作为上下文发送给 LLM
- 支持流式（SSE）和非流式两种模式
- 前端采用 requestAnimationFrame 批量渲染，避免界面抖动
- 思考过程分阶段可视化："检索知识库" → "AI 正在生成回答"
- 模糊问题时 LLM 先反问澄清
- 回答后支持停止生成、重新生成、保存为笔记

#### 3.3.3 知识图谱模块
- 将笔记内容发送给 LLM，提取概念和关系（前置/扩展/相关/矛盾）
- 支持可视化展示（ECharts 关系图）
- 知识点统计

#### 3.3.4 学习计划模块
- 用户输入学习目标
- LLM 自动生成结构化的学习路径（分阶段、分主题）
- 用户可标记任务完成状态

#### 3.3.5 复习模块
- 实现 SM-2 间隔重复算法
- 创建笔记时自动生成复习提醒（首次复习为次日）
- 根据用户自评质量动态调整复习间隔
- 学习打卡记录

#### 3.3.6 智能辅助模块
- 文本摘要：LLM 压缩为 3-5 条中文要点
- 难度评估：beginner / intermediate / advanced
- 智能标签推荐：根据笔记内容推荐分类和标签
- 学情诊断：聚合学习数据，AI 分析后给出个性化建议

### 3.4 数据库设计

#### 3.4.1 MySQL 业务数据库

主要数据表：

- **users**：用户信息（id, username, password, email, avatar, role, created_at, updated_at, deleted）
- **notes**：笔记（id, user_id, title, content_md, category_id, difficulty_level, created_at, updated_at, deleted）
- **categories**：分类（id, user_id, name, color, created_at, deleted）
- **tags**：标签（id, user_id, name, created_at, deleted）
- **note_tags**：笔记标签关联（note_id, tag_id）
- **learning_plans**：学习计划（id, user_id, title, description, goal, start_date, end_date, status, ai_generated, created_at, updated_at, deleted）
- **plan_items**：计划任务（id, plan_id, title, description, note_id, order_num, estimated_minutes, completed, completed_at, created_at）
- **learning_records**：学习记录（id, user_id, note_id, duration_minutes, date, created_at）
- **review_reminders**：复习提醒（id, user_id, note_id, next_review_at, interval_days, ease_factor, repetitions, created_at, updated_at）
- **knowledge_relations**：知识关系（id, note_id, source, target, relation_type, weight, created_at）
- **chat_history**：聊天记录（id, user_id, role, content, note_id, created_at）

#### 3.4.2 ChromaDB 向量数据库

- **knowledge_notes** 集合：存储笔记的向量嵌入
  - id: note_id（或 chat_user_{userId}_{timestamp} 用于聊天记忆）
  - document: 文本内容（笔记为 "Title: {title}\n\n{content}"，聊天记忆为 "用户提问: {question}\nAI回答: {answer}"）
  - metadata: user_id, note_id, type（chat_memory / 笔记）
  - embedding: Sentence Transformers 生成的向量
  - 距离度量：余弦相似度（cosine）

---

## 4. 系统实现

### 4.1 前端实现

#### 4.1.1 项目结构

前端采用 Vue 3 组合式 API（Composition API）和 `<script setup>` 语法：

```
frontend/
├── src/
│   ├── api/           # API 接口（ai.ts, notes.ts, dashboard.ts 等）
│   ├── components/    # 通用组件
│   ├── layouts/       # 布局组件（MainLayout.vue）
│   ├── router/        # 路由配置（含导航守卫鉴权）
│   ├── stores/        # Pinia 状态管理（user.ts）
│   ├── styles/        # 全局样式与主题变量
│   └── views/         # 页面视图
│       ├── AiAssistant/Index.vue    # AI 助手（核心页面）
│       ├── Dashboard/Index.vue      # 学习仪表盘
│       ├── Notes/Index.vue          # 笔记列表
│       ├── NoteEditor/Index.vue     # 笔记编辑器
│       ├── KnowledgeGraph/Index.vue # 知识图谱
│       ├── LearningPlans/Index.vue  # 学习计划
│       ├── Categories/Index.vue     # 分类管理
│       ├── Tags/Index.vue           # 标签管理
│       ├── Login/Index.vue          # 登录页
│       └── Profile/Index.vue        # 个人中心
```

#### 4.1.2 路由与鉴权

使用 Vue Router 4 实现路由管理，通过导航守卫进行鉴权：

```typescript
router.beforeEach((to, _from, next) => {
  const userStore = useUserStore()
  if (to.meta.guest) {
    next()
  } else if (!userStore.token) {
    next('/login')
  } else {
    next()
  }
})
```

#### 4.1.3 流式问答（SSE + rAF 批量渲染）

AI 助手页面是系统使用频率最高的功能。为了提供流畅的交互体验，本系统实现了以下关键技术：

**SSE 流式接收**：通过 fetch API 发送 POST 请求，接收 `text/event-stream` 格式的 SSE 数据流。使用 `ReadableStream.getReader()` 逐块读取，按行解析 `data:` 前缀的 SSE 协议数据。支持 `AbortController` 取消正在进行的请求。

**requestAnimationFrame 批量渲染**：在流式接收过程中，LLM token 可能以极高频率到达（每 token ~10-50ms）。如果每次收到 token 都直接更新 DOM，会导致每秒 50-100 次的重绘，造成严重的界面抖动。本系统采用 requestAnimationFrame 将同一帧内到达的多个 token 合并在一次渲染中输出，保持稳定的 60fps 刷新率。

**渐进式 Markdown 渲染**：在流式输出期间，始终通过 `marked()` 将累积文本实时渲染为 HTML 格式，避免了"原始 Markdown 语法"与"渲染后 HTML"之间交替闪变造成的视觉抖动。

```typescript
// 发送 & 流式接收核心逻辑
async function send() {
  const q = input.value.trim()
  if (!q || loading.value) return

  // 1. 构建短期记忆：最近 10 条对话历史
  const recentHistory: HistoryMsg[] = messages.value
    .slice(0, -2).slice(-10)
    .map(m => ({ role: m.role, content: m.content.replace(/<[^>]*>/g, '') }))

  // 2. 流式接收
  let raw = ''
  let renderPending = false
  for await (const chunk of aiChatStream(userId, q, undefined, abortController.signal, recentHistory)) {
    if (typeof chunk === 'string') {
      raw += chunk
      // requestAnimationFrame 合并同一帧内的多次更新
      if (!renderPending) {
        renderPending = true
        requestAnimationFrame(async () => {
          renderPending = false
          messages.value[idx].content = await marked(raw)
          if (!userScrolledUp.value) {
            msgContainer.value.scrollTop = msgContainer.value.scrollHeight
          }
        })
      }
    } else {
      // chunk 为 StreamMeta：contains should_save flag
      streamMeta = chunk
    }
  }

  // 3. 后处理：聊天记忆同步 + 保存提示
  syncChatMemory(userId, q, raw)
  if (streamMeta?.should_save) {
    // 弹窗询问用户是否保存为笔记
  }
}
```

**思考过程可视化**：加载状态分为两个阶段展示——"检索知识库"（带搜索图标，检索完成后显示 ✓）+ "AI 正在生成回答"（带加载动画，生成中显示跳动圆点）。AI 头像在思考期间带有呼吸光晕脉冲动画。

**停止生成 / 重新生成**：流式输出过程中，发送按钮变为红色停止按钮。点击后通过 AbortController 取消请求。每条 AI 回答旁提供重新生成按钮，会删除当前回答并用原始问题重新请求。

**保存为笔记**：每条 AI 回答旁增加了保存按钮，点击后自动提取问题为标题、问答内容为正文，创建新笔记并同步向量至 ChromaDB。当向量库中无相关知识时，系统会自动弹出保存提示。

**智能滚动**：流式输出时自动跟随最新内容滚动。当用户手动向上滚动查阅历史消息时，停止强制滚动，并在右下角显示"↓ 回到底部"浮动按钮。

#### 4.1.4 学习仪表盘

仪表盘是用户进入系统后的首页，提供了以下信息：

- **统计卡片**：笔记总数、今日学习时长、知识节点数、待复习数
- **学习趋势图**（ECharts 折线图）：展示最近 7 天学习时长变化
- **知识分布图**（ECharts 环形饼图）：按分类统计笔记数量
- **最近笔记列表**：显示最新 5 篇笔记及其分类和难度
- **待复习列表**：显示近期需复习的笔记及完成按钮
- **AI 学情诊断**：聚合用户的笔记数、学习时长、复习数据、分类分布等，发送至 AI Agent 分析，返回个性化学习总结、薄弱点分析、下一步建议和鼓励语

#### 4.1.5 前端 API 层

前端通过 Axios 实例统一管理 API 请求，自动附加 JWT Token。流式接口使用原生 fetch API 以支持 ReadableStream。API 层与视图层分离，所有接口调用集中在 `src/api/` 目录。

### 4.2 Spring Boot 业务层实现

#### 4.2.1 AI 代理转发

Spring Boot 不仅是业务逻辑的处理中心，也是前端与 AI Agent 之间的代理层。使用 Spring WebClient（基于 Reactor）实现响应式 HTTP 通信：

- **普通请求**：通过 `bodyToMono(String.class).block()` 同步获取 AI Agent 响应并解析为 JSON 返回前端
- **流式请求**：通过 `bodyToFlux(String.class)` 获取 SSE 流，使用 `doOnNext` 规范化 `data:` 前缀后透传给前端 `HttpServletResponse`。规范化处理兼容 AI Agent 不同版本可能发出的带或不带 `data:` 前缀的数据

```java
// SSE 流代理实现
aiAgentClient.chatStream(userId, noteId, question, history)
    .doOnNext(data -> {
        String line = data.trim();
        if (line.isEmpty()) return;
        if (line.startsWith("data: ")) line = line.substring(6);
        writer.write("data: " + line + "\n\n");
        writer.flush();
    })
    .doOnError(e -> {
        writer.write("data: [ERROR] " + e.getMessage() + "\n\n");
        writer.flush();
    })
    .blockLast();
```

#### 4.2.2 学情诊断数据聚合

`/api/ai/learning-insight` 端点在 Spring Boot 层完成跨表数据聚合：

- 查询 notes 表获取笔记总数
- 查询 learning_plans 表获取活跃计划数
- 查询 learning_records 表统计今日学习时长
- 查询 review_reminders 表获取待复习数和近期复习列表
- 查询 categories 表获取分类分布
- 查询 knowledge_relations 表统计知识节点数

聚合后的数据以 Map 形式发送至 AI Agent 的 `/agent/learning-insight` 端点进行分析。

#### 4.2.3 复习自动创建

在笔记创建时（`NoteController.create()`），自动调用 `ReviewReminderService.create()` 为笔记生成一条初始复习提醒（间隔 1 天，难度因子 2.5），实现了"创建即复习"的闭环。

### 4.3 AI Agent 实现

AI Agent 是整个系统的智能核心，采用 FastAPI 框架单独部署，通过 LangChain 接入 DeepSeek API。

#### 4.3.1 向量存储服务

使用 ChromaDB 持久化存储笔记和聊天记忆的向量嵌入，采用线程安全单例模式：

```python
# vector_store.py - 向量数据库初始化
def get_collection():
    client = chromadb.PersistentClient(path=CHROMA_PERSIST_DIR, settings=Settings(anonymized_telemetry=False))
    ef = embedding_functions.SentenceTransformerEmbeddingFunction(
        model_name=EMBEDDING_MODEL  # paraphrase-multilingual-MiniLM-L12-v2
    )
    return client.get_or_create_collection(
        name=CHROMA_COLLECTION,
        embedding_function=ef,
        metadata={"hnsw:space": "cosine"},
    )
```

#### 4.3.2 RAG 问答服务

实现了完整的 RAG（检索增强生成）问答流程：**并行检索 → 上下文构建 → Prompt 注入 → LLM 生成 → 元数据返回**。

**检索阶段**：使用 `ThreadPoolExecutor` 并行检索笔记向量库和聊天记忆向量库，各设置 30 秒超时，任一失败不影响另一路。支持按 `user_id` 做多租户隔离，按 `note_id` 过滤指定笔记。

**上下文构建**：将检索到的笔记内容（最多 3 条，每条截断至 500 字符）和聊天记忆（最多 3 条，每条截断至 300 字符）拼接为上下文文本。同时格式化前端传入的对话历史（最近 10 条，每条截断至 300 字符）。

**Prompt 设计**：系统使用两套 Prompt 模板：
- `RAG_PROMPT`：当向量库有相关知识时使用。System Prompt 指导 LLM 基于上下文回答，保持准确性和连续性，遇到模糊问题时先反问澄清
- `DIRECT_PROMPT`：当向量库无相关知识时使用。System Prompt 指导 LLM 作为通用助手回答，同时记住之前的对话内容，对模糊问题进行追问

```python
RAG_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """You are a helpful AI assistant with memory...
## Core rules
1. Use the retrieved context for factual accuracy.
2. If the context doesn't contain relevant info, provide a general answer.
3. IMPORTANT — Clarify before answering: If the user's question is vague or broad,
   ask 1-2 clarifying questions instead of giving a generic answer.
4. Keep responses concise. Use Chinese."""),
    ("human", "Conversation history:\n{history}\n\nKnowledge context:\n{context}\n\nQuestion: {question}"),
])
```

**生成阶段**：使用 LangChain 的 LCEL（LangChain Expression Language）管道语法 `prompt | llm` 构建调用链，通过 `streaming_llm.stream()` 逐 token 生成回复。

**元数据返回**：流式输出的末尾 yield 一个包含 `[DONE]` 标记和 JSON 元数据的特殊 token，前端据此判断流结束并提取 `should_save`、`suggested_title` 等字段。

```python
def rag_chat_stream(user_id, question, note_id=None, history=None):
    context, has_notes = _build_context(user_id, question, note_id)
    history_text = _format_history(history)

    # 选择 Prompt 并流式生成
    if context:
        chain = RAG_PROMPT | streaming_llm
        for chunk in chain.stream({"history": history_text, "context": context, "question": question}):
            yield chunk.content
    else:
        chain = DIRECT_PROMPT | streaming_llm
        for chunk in chain.stream({"history": history_text, "question": question}):
            yield chunk.content

    # 末尾附上元数据（前端的 should_save 据此弹窗）
    import json
    meta = {"should_save": not has_notes, "suggested_title": question[:50]}
    yield "[DONE]" + json.dumps(meta, ensure_ascii=False)
```

#### 4.3.3 短期记忆与长期记忆

**短期记忆**：前端在每次发送时将当前会话最近 10 条消息（已脱 HTML 标签）组装为 `history` 数组随请求发送。AI Agent 的 `_format_history()` 将其格式化为 `"用户: ...\n助手: ..."` 的对话记录，注入 Prompt 的 `{history}` 占位符。这使得 LLM 在每次回答时都知晓"刚才聊了什么"，能够正确理解代词指代、追问、引用之前讨论的内容。

**长期记忆**：每轮对话结束后，前端异步调用 `syncChatMemory()` 将问答内容存入 ChromaDB（以 `chat_user_{userId}_{timestamp}` 为 ID）。在下一次提问时，`_build_context()` 通过语义检索找到相关的历史对话片段，作为知识上下文的补充。

#### 4.3.4 智能标签推荐

`/agent/suggest-tags` 端点接收笔记内容，调用 LLM 分析后返回 JSON：

```json
{
  "category": "AI/机器学习",
  "tags": ["深度学习", "Transformer", "注意力机制"],
  "reason": "内容涉及神经网络中的注意力机制原理"
}
```

#### 4.3.5 学情诊断

`/agent/learning-insight` 端点接收 Spring Boot 聚合的学习数据，调用 LLM 生成个性化分析：

```json
{
  "summary": "你目前有 12 篇笔记，主要关注 AI 和后端方向...",
  "strengths": ["学习频率稳定", "笔记覆盖多领域"],
  "weaknesses": ["缺少系统性复习"],
  "review_today": ["笔记#3", "笔记#7"],
  "next_step": "建议深入学习 Transformer 架构的注意力机制",
  "motivation": "坚持每天学习，进步明显！"
}
```

#### 4.3.6 SM-2 间隔重复算法

实现了经典的 SM-2 算法：

```python
def calculate_next_review(quality, interval_days, ease_factor):
    if quality >= 3:
        if interval_days == 1:  next_interval = 1
        elif interval_days == 2:  next_interval = 6
        else:  next_interval = round(interval_days * ease_factor)
        new_ef = ease_factor + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02))
        new_ef = max(new_ef, 1.3)
    else:
        next_interval = 1
        new_ef = ease_factor
    return next_interval, new_ef
```

### 4.4 AI Agent API 端点汇总

| 端点 | 方法 | 功能 |
|------|------|------|
| `/agent/chat` | POST | 非流式 RAG 问答 |
| `/agent/chat/stream` | POST | 流式 RAG 问答（SSE），末尾附带 should_save 元数据 |
| `/agent/embeddings/sync` | POST | 同步笔记向量嵌入 |
| `/agent/search-notes` | POST | 语义搜索笔记 |
| `/agent/summarize` | POST | 文本摘要 |
| `/agent/assess-difficulty` | POST | 难度评估 |
| `/agent/suggest-tags` | POST | 智能标签/分类推荐 |
| `/agent/extract-graph` | POST | 知识图谱提取 |
| `/agent/gen-learning-plan` | POST | 学习计划生成 |
| `/agent/next-review` | POST | SM-2 复习间隔计算 |
| `/agent/learning-insight` | POST | 学情诊断分析 |
| `/agent/chat-memory/sync` | POST | 聊天记忆同步至向量库 |
| `/agent/chat-memory/search` | POST | 聊天记忆语义检索 |
| `/health` | GET | 健康检查 |

### 4.5 RAG 问答完整数据流

```
用户输入问题 "什么是机器学习？"
     │
     ▼
前端 AiAssistant/Index.vue
  ├─ send() 构建 recentHistory（最近10条消息）
  ├─ aiChatStream(userId, q, _, abortSignal, history)
  │
     ▼
前端 ai.ts: fetch POST /api/ai/chat/stream
  body: { userId, question, history }
     │
     ▼  Spring Boot AiController.chatStream()
     │   └─ WebClient → FastAPI /agent/chat/stream
     │
     ▼  AI Agent chat.py → rag_service.py
     │   ├─ _build_context()    并行检索笔记+聊天记忆 (ChromaDB)
     │   ├─ _format_history()   格式化短期记忆
     │   ├─ 选择 Prompt 模板    RAG_PROMPT 或 DIRECT_PROMPT
     │   └─ streaming_llm.stream() → DeepSeek API
     │
     ▼  SSE 流返回
Spring Boot: 规范化 data: 前缀 → writer.write() → 前端
     │
     ▼ 前端接收
  ├─ ReadableStream.getReader() 逐行解析 SSE
  ├─ requestAnimationFrame 批量 marked() 渲染
  ├─ 解析 [DONE]{json} → should_save 判断
  ├─ syncChatMemory() 异步存储聊天记忆
  └─ should_save 为 true → 弹窗询问 → createNote()
```

---

## 5. 系统测试

### 5.1 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| 前端运行 | Node.js 18+, Vite |
| 后端运行 | JDK 17, Spring Boot 3.x |
| AI Agent | Python 3.10+, FastAPI |
| 向量数据库 | ChromaDB（本地持久化） |
| 关系数据库 | MySQL 8.0 |
| 浏览器 | Chrome 120+ |
| LLM 服务 | DeepSeek API（deepseek-chat） |

### 5.2 功能测试

| 测试用例 | 预期结果 | 实际结果 |
|----------|---------|---------|
| 用户注册登录 | 成功注册并获取 JWT Token | ✅ 通过 |
| 创建笔记 | 笔记保存到 MySQL，向量同步到 ChromaDB，自动创建复习提醒 | ✅ 通过 |
| RAG 问答（有笔记） | 基于笔记内容给出准确回答 | ✅ 通过 |
| RAG 问答（无笔记） | 通用回答 + 弹出保存提示 | ✅ 通过 |
| 流式问答 | SSE 逐 token 输出，界面无抖动 | ✅ 通过 |
| 短期记忆 | 能记住当前会话中的对话内容 | ✅ 通过 |
| 追问引导 | 模糊问题时反问澄清 | ✅ 通过 |
| 停止生成 | 点击停止后 AI 立即停止输出 | ✅ 通过 |
| 重新生成 | 用原问题重新获取回答 | ✅ 通过 |
| 保存为笔记 | 问答内容正确保存为笔记 | ✅ 通过 |
| 知识图谱提取 | 返回结构化的概念关系 JSON | ✅ 通过 |
| 复习间隔计算 | 根据 SM-2 算法返回正确间隔 | ✅ 通过 |
| 学习计划生成 | 生成结构化的学习路径 | ✅ 通过 |
| 语义搜索 | 返回语义相关的结果 | ✅ 通过 |
| 智能标签推荐 | 根据内容推荐分类和标签 | ✅ 通过 |
| 学情诊断 | 聚合学习数据并给出 AI 建议 | ✅ 通过 |

---

## 6. 总结与展望

### 6.1 工作总结

本文设计并实现了一个智能知识管理助手系统，主要工作包括：

1. **架构设计**：采用前后端分离 + AI Agent 独立部署的三层架构，Spring Boot 作为代理层统一对外接口，各层职责清晰
2. **RAG 智能问答**：实现了完整的检索增强生成流程，包括并行多路检索（笔记+聊天记忆）、短期记忆（会话历史注入）、长期记忆（向量化聊天存储）、流式 SSE 输出、requestAnimationFrame 批量渲染、思考过程可视化、停止/重新生成、一键保存为笔记等功能
3. **AI 能力集成**：基于 LangChain 框架接入 DeepSeek 大语言模型，实现了 RAG 问答、知识图谱提取、学习计划生成、文本摘要、难度评估、智能标签推荐、学情诊断等 7 项 AI 能力
4. **向量检索**：使用 ChromaDB + Sentence Transformers 存储笔记嵌入和聊天记忆，实现了语义级的检索能力，本地运行保护用户隐私
5. **间隔重复**：实现了 SM-2 算法，创建笔记时自动生成复习提醒，完成复习后动态调整间隔
6. **前端体验优化**：实现了 SSR 流式渲染、rAF 批量 DOM 更新、思考过程分阶段展示、智能滚动、消息入场动画、自适应输入框等交互细节

### 6.2 不足与展望

1. **多模态支持**：当前仅支持文本笔记，后续可扩展图片 OCR、语音笔记等功能
2. **知识图谱增强**：可引入更精确的实体识别模型，提升知识图谱质量
3. **推荐系统**：可根据用户的学习历史推荐相关的笔记和学习资源
4. **移动端适配**：当前界面主要适配桌面端，后续可优化移动端体验
5. **离线能力**：可考虑在本地部署小型模型（如 Ollama），提供基本的离线问答能力
6. **Agent 能力增强**：可引入 Function Calling，让 AI 能够主动操作笔记（搜索、创建、更新）

---

## 参考文献

[1] Vaswani A, Shazeer N, Parmar N, et al. Attention is all you need[C]. NeurIPS, 2017.

[2] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks[C]. NeurIPS, 2020.

[3] Wozniak P A, Gorzelanczyk E J. Optimization of repetition spacing in the practice of learning[J]. Acta Neurobiologiae Experimentalis, 1994, 54(1): 57-62.

[4] LangChain. LangChain Documentation[EB/OL]. https://python.langchain.com/, 2024.

[5] Chroma. ChromaDB Documentation[EB/OL]. https://docs.trychroma.com/, 2024.

[6] Reimers N, Gurevych I. Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks[C]. EMNLP, 2019.

[7] DeepSeek. DeepSeek API Documentation[EB/OL]. https://platform.deepseek.com/, 2024.

[8] You Y. Vue 3 Documentation[EB/OL]. https://vuejs.org/, 2024.

[9] 陈昊. 基于知识图谱的个性化学习路径推荐系统研究[D]. 北京邮电大学, 2023.

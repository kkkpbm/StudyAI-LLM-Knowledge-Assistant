# 基于大语言模型的智能知识管理助手的设计与实现 —— 选题报告

---

## 一、题目与来源

**题目**：基于大语言模型的智能知识管理助手的设计与实现

**题目类型**：毕业设计（软件开发类）

**课题来源**：自拟课题

**学生专业**：软件工程

---

## 二、课题简介

随着互联网信息量的爆炸式增长，个人知识管理变得越来越重要。传统的笔记工具（如 Notion、印象笔记等）仅提供简单的文本存储和分类功能，缺乏智能化的知识组织与检索能力——笔记写完即搁置、关键词搜索无法理解语义、不同笔记之间形成知识孤岛，用户难以实现高效的知识内化与体系构建。

本课题旨在设计并实现一个**基于大语言模型的智能知识管理助手系统**，将大语言模型的自然语言理解与生成能力同用户私有知识库深度融合，从传统的"被动记录工具"升级为"主动学习助手"。系统采用前后端分离的三层架构：前端基于 Vue 3 + TypeScript 构建用户界面，后端采用 Spring Boot 提供业务 API，AI Agent 层基于 FastAPI + LangChain + DeepSeek 实现智能功能，并结合 ChromaDB 向量数据库存储笔记嵌入和聊天记忆。

系统核心功能包括：（1）基于 RAG 的智能问答，支持流式 SSE 输出、短期记忆与长期记忆双通道机制、追问引导、回答一键保存为笔记；（2）知识图谱自动提取与可视化，从笔记内容中提取概念及其关系网络；（3）AI 学习计划生成与进度跟踪；（4）基于 SM-2 间隔重复算法的科学复习管理；（5）学习仪表盘与 AI 学情诊断；（6）文本摘要、难度评估、智能标签推荐等辅助功能。

该系统将笔记管理、RAG 问答、知识图谱、学习规划、间隔重复、AI 诊断整合为一个完整的学习闭环，帮助用户高效地存储、检索、理解、组织和复习知识，提升学习效率和知识留存率。

---

## 三、选题背景与意义

### 2.1 选题背景

在信息爆炸的时代，个人知识管理的需求日益迫切。学生每天需要在课堂笔记、阅读材料、在线课程之间切换，职场人士也需要持续学习新技术并构建知识体系。然而，当前主流笔记工具（Notion、印象笔记、OneNote 等）普遍存在以下局限：

1. **被动存储**：笔记以静态文本形式存放，系统不会主动帮助用户理解、关联、内化知识
2. **检索浅层化**：关键词搜索无法理解语义，用户输入"怎么实现排序"无法匹配到"快速排序算法"相关笔记
3. **缺乏学习闭环**：笔记写完即搁置，缺少复习提醒和学习路径引导，用户容易遗忘
4. **知识孤岛**：不同笔记之间缺乏关联，难以形成系统化的知识网络

2022 年底以来，以 ChatGPT 为代表的大语言模型（LLM）取得了突破性进展，展现出强大的自然语言理解和生成能力。2023-2024 年间，检索增强生成（RAG）、智能体（Agent）、向量数据库等技术日趋成熟，为构建下一代智能知识管理工具提供了技术基础。利用这些技术，笔记工具可以从"被动容器"升级为"主动助手"——帮助用户检索、理解、关联、复习知识。

### 2.2 选题意义

本课题的理论意义和实践意义如下：

**理论意义**：
- 探索 RAG 技术在垂直领域（个人知识管理）的应用模式，验证向量检索 + LLM 生成在私域知识问答中的有效性
- 将间隔重复理论（SM-2 算法）与 AI 能力结合，探索融合认知科学规律的智能学习系统
- 探索"短期记忆（会话历史注入）+ 长期记忆（向量化存储检索）"双通道记忆机制在 AI 对话系统中的应用

**实践意义**：
- 为用户提供自然的语义问答体验，用日常语言即可搜索和询问笔记中的知识
- 自动提取知识概念及其关系，帮助用户发现知识间的隐式关联
- 根据学习目标自动生成个性化学习路径，降低自学门槛
- 基于科学算法安排复习，提升学习效率和知识留存率
- AI 回答可一键保存为笔记，形成知识正向积累的闭环

---

## 四、国内外研究现状

### 4.1 知识管理工具

国外方面，Notion 提供了高度灵活的内容组织方式（数据库、页面嵌套），但仍依赖人工整理，缺乏 AI 驱动的内容理解。Roam Research 和 Obsidian 引入双向链接和知识图谱视图，帮助用户构建笔记网络，但不具备自动提取知识和智能问答的能力。

国内方面，印象笔记、有道云笔记、飞书文档等以文档管理和协作为核心，功能集中于富文本编辑、多端同步、团队协作，在 AI 辅助学习方面功能有限。2023 年以来，部分工具开始集成 AI 总结和 AI 搜索功能，但多停留在对单篇文档的摘要和关键词提取层面。

### 4.2 AI 辅助学习

Anki 等间隔重复工具基于 SM-2 系列算法帮助用户高效记忆，但需要用户手动制作卡片，制作成本较高。Quizlet、Memrise 等引入了 AI 自动生成题目，但未与用户自己的知识库深度结合。

ChatGPT、Claude 等通用对话模型被广泛用于辅助学习，但直接使用存在两个核心问题：一是模型的训练数据截止，无法访问用户最新的学习资料；二是缺乏用户私域知识的上下文，无法给出个性化的解答。RAG 技术正是为了解决这两个问题而产生的。

### 4.3 RAG 技术

Lewis 等人于 2020 年提出 RAG 架构[2]，将检索系统与生成模型结合：先从外部知识库检索相关文档片段，再将检索结果作为上下文输入 LLM 生成回答。LangChain[4] 和 LlamaIndex 等框架提供了标准化的 RAG 实现组件，大幅降低了开发门槛。向量数据库（ChromaDB[5]、Milvus、Pinecone 等）为语义级检索提供了高效存储和查询能力。本地嵌入模型（如 Sentence Transformers[6]）的发展使得离线向量化成为可能，无需依赖外部 API。

### 4.4 现有工作的不足

综上所述，当前市场上尚缺乏将"笔记管理 + RAG 问答 + 知识图谱 + 学习规划 + 间隔重复 + 记忆系统 + AI 学习诊断"完整整合的系统。现有工具或偏向内容管理（缺乏 AI 洞察），或偏向通用对话（缺乏私有知识上下文），二者之间存在明显的产品空白。

---

## 五、研究目标与研究内容

### 5.1 研究目标

设计并实现一个智能知识管理助手系统，将大语言模型的自然语言能力与用户私有知识库深度融合，帮助用户高效地存储、检索、理解、组织和复习知识。

### 5.2 研究内容

本课题的研究内容分为以下七个方面：

**(1) RAG 智能问答子系统**
实现基于用户笔记库的检索增强生成问答。用户以自然语言提问，系统从向量数据库中语义检索相关笔记片段和聊天历史，构造增强上下文，交由 LLM 生成领域相关、个性化的回答。支持流式 SSE 输出，前端采用 requestAnimationFrame 批量渲染保证界面流畅性。集成短期记忆（会话历史注入 Prompt）和长期记忆（聊天记录向量化存储检索），使 AI 具备对话连续性。模糊问题时 LLM 先反问澄清，避免给出笼统无用的回答。

**(2) 笔记向量化与语义搜索子系统**
将用户笔记文本通过 Sentence Transformers 多语言嵌入模型编码为向量，持久化存储在 ChromaDB 向量数据库中。支持按 user_id 实现多租户隔离。提供语义搜索接口，按照余弦相似度返回 top-k 最相关结果。

**(3) 知识图谱提取子系统**
利用 LLM 从笔记文本中自动提取概念及其关系（前置、扩展、相关、矛盾），构建结构化的知识网络。支持关系强度权重标注（0.0-1.0），为可视化展示和知识体系构建提供数据基础。

**(4) 学习计划生成子系统**
根据用户输入的学习目标，由 LLM 生成结构化的分阶段学习路径，包含各阶段的名称、描述、主题列表、预计天数和资源建议。支持用户标记任务完成状态，跟踪学习进度。

**(5) 间隔重复复习子系统**
实现 SuperMemo SM-2 算法，根据用户对每条笔记记忆质量的自我评分（0-5），动态计算最优复习间隔天数，并调整难度因子。创建笔记时自动生成初始复习提醒，实现"创建即复习"闭环。

**(6) 智能辅助子系统**
包括：文本自动摘要（LLM 压缩为中文要点）、学习材料难度评估（初学者/中级/高级）、智能标签推荐（根据笔记内容推荐分类和标签）、学情诊断（聚合学习数据后由 AI 分析给出个性化建议）。

**(7) 交互体验优化**
包括：流式输出时的 requestAnimationFrame 批量渲染消除界面抖动、思考过程分阶段可视化（"检索知识库"→"AI 生成回答"）、停止生成与重新生成、智能滚动（流式输出时自动跟底，用户上滑后停止跟随）、自适应输入框（支持 Shift+Enter 换行）、消息入场动画等。

### 5.3 非功能需求

| 指标 | 要求 |
|------|------|
| 普通 API 响应时间 | < 2 秒 |
| 流式问答首 token 延迟 | < 3 秒 |
| 流式输出渲染频率 | 60fps（requestAnimationFrame 驱动） |
| 语义搜索响应时间 | < 1 秒 |
| 系统可用性 | 前后端独立部署，核心服务可独立扩展 |
| 安全性 | JWT Token 认证，接口鉴权 |
| 兼容性 | 支持主流浏览器 Chrome、Edge、Firefox |

### 5.4 研究方向

本课题的研究方向主要涵盖以下几个方面：

**（1）检索增强生成（RAG）在垂直领域的应用**

研究如何将 RAG 技术应用于个人知识管理场景，包括文档切片策略、向量化嵌入方案、多路并行检索（笔记向量 + 聊天记忆检索）、检索结果重排序以及与 LLM Prompt 的上下文融合机制。

**（2）LLM 记忆机制设计**

研究"短期记忆 + 长期记忆"双通道记忆架构在 AI 对话系统中的应用，探索会话历史 Prompt 注入与向量化记忆检索的协同工作原理，解决跨会话对话连续性问题。

**（3）知识图谱的自动化构建**

研究利用 LLM 从非结构化文本中自动提取知识实体及其关系（前置、扩展、相关、矛盾）的方法，探索结构化知识网络的自动生成与可视化展示。

**（4）认知科学与 AI 的融合**

研究 SM-2 间隔重复算法与 LLM 的协同应用，将认知科学中关于记忆与遗忘的规律嵌入智能学习系统，实现科学的复习调度与个性化学习路径规划。

**（5）流式交互体验优化**

研究 SSE 流式传输在前端的渲染优化策略，包括 requestAnimationFrame 批量渲染、思考过程分阶段可视化、Markdown 实时增量渲染等，解决高频 token 更新导致的界面抖动问题。

---

## 六、拟解决的关键问题

1. **检索质量优化**：如何从用户异构笔记中准确检索出最相关的片段？本系统采用多路并行检索（笔记向量 + 聊天记忆），设置超时降级机制，并通过 note_id 指定过滤提升相关性。

2. **LLM 幻觉控制**：如何在利用 LLM 生成能力的同时限制其编造事实？通过严格的 Prompt 工程设计，要求 LLM 明确基于上下文回答，并在上下文不足时如实声明。

3. **对话连续性**：如何让 AI 记住之前聊了什么？采用"短期记忆 + 长期记忆"双通道方案：短期记忆将会话最近消息注入 Prompt，长期记忆通过 ChromaDB 语义检索相关历史对话。

4. **流式输出流畅性**：如何避免高频 token 更新导致的界面抖动？采用 requestAnimationFrame 将同一帧内到达的多个 token 合并在一次 DOM 更新中渲染，始终通过 marked() 渲染 Markdown 而非交替显示原始语法。

5. **知识图谱的准确性**：LLM 提取的概念关系可能存在错误或遗漏。通过严格的 JSON 输出格式约束和异常解析兜底来保证数据可用性。

6. **多租户数据隔离**：不同用户的知识库必须严格隔离。向量存储通过 metadata 中的 user_id 过滤实现，MySQL 通过数据表 user_id 外键约束实现。

---

## 七、技术路线与总体方案

### 7.1 系统总体架构

系统采用**前后端分离 + AI Agent 独立部署**的三层架构，Spring Boot 作为前端与 AI Agent 之间的代理层：

```
┌──────────────────────────────────────────┐
│          前端展示层（Vue 3 + TS）          │
│   Element Plus + ECharts + Vditor         │
└─────────────┬────────────────┬────────────┘
              │                │
      HTTP/REST          HTTP/REST (SSE)
              ▼                ▼
┌──────────────────┐  ┌─────────────────────┐
│  业务服务层       │  │  AI Agent 服务层     │
│  (Spring Boot)   │  │  (FastAPI +         │
│  ─────────────── │  │   LangChain)        │
│  · 用户认证 JWT  │  │  ───────────────    │
│  · 笔记 CRUD     │  │  · RAG 问答         │
│  · 分类/标签管理 │  │  · 知识图谱         │
│  · 学习计划管理  │  │  · 学习计划生成     │
│  · 复习/打卡记录 │  │  · 文本摘要         │
│  · 数据统计      │  │  · 语义搜索         │
│  · AI 代理转发   │  │  · 聊天记忆         │
│                  │  │  · 标签推荐         │
│  ← MySQL ────────│  │  · 学情诊断         │
└──────┬───────────┘  └──────────┬──────────┘
       │                         │
       ▼                         ▼
┌──────────────┐      ┌──────────────────┐
│   MySQL      │      │   ChromaDB       │
│  (业务数据)   │      │  (向量嵌入存储)   │
│              │      │  + Sentence      │
│              │      │  Transformers    │
└──────────────┘      └──────────────────┘
```

**架构说明**：
- 前端负责 UI 交互，通过 Axios（普通请求）和 fetch（SSE 流）与后端通信
- Spring Boot 负责结构化业务逻辑（用户、笔记、计划），操作 MySQL 数据库，同时作为 AI Agent 的代理层转发 AI 请求
- AI Agent 负责所有 AI 相关能力，操作 ChromaDB 向量数据库，调用 DeepSeek API
- 前后端通过 JWT Token 传递用户身份

### 7.2 技术选型

| 层次 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| 前端框架 | Vue 3 + TypeScript | 3.x | 组合式 API 便于逻辑复用；TypeScript 保证类型安全 |
| 构建工具 | Vite | 5.x | 冷启动快，HMR 极速 |
| UI 组件库 | Element Plus | 2.x | 成熟的中文生态，组件丰富 |
| 状态管理 | Pinia | 2.x | Vue 3 官方推荐，API 简洁 |
| Markdown 编辑 | Vditor | 3.x | 支持所见即所得，中文支持好 |
| 图表库 | ECharts | 5.x | 强大的可视化能力 |
| 业务后端框架 | Spring Boot | 3.x | 成熟的 Java 企业级框架，生态完善 |
| ORM | MyBatis-Plus | 3.x | 简化数据库操作，Lambda 查询 |
| 服务间通信 | Spring WebClient (Reactor) | - | 响应式 HTTP 客户端，支持 SSE 流代理 |
| AI 服务框架 | FastAPI | 0.109 | 高性能异步 Python 框架，自动生成 API 文档 |
| LLM 应用框架 | LangChain | 0.2+ | 成熟的 LLM 编排框架，支持 Prompt 模板、LCEL 管道 |
| 大语言模型 | DeepSeek API | deepseek-chat | 高性价比的中文大语言模型 |
| 向量数据库 | ChromaDB | 0.5+ | 轻量级、本地化部署、零成本 |
| 嵌入模型 | Sentence Transformers | paraphrase-multilingual-MiniLM-L12-v2 | 多语言支持、本地运行、无需 API 费用 |
| 关系数据库 | MySQL | 8.0 | 稳定的关系型数据库 |

### 7.3 关键技术说明

**RAG 核心流程**：
1. 用户提问 → 前端构建短期记忆（最近 10 条对话）随请求发送
2. AI Agent 并行检索笔记向量库和聊天记忆向量库（ThreadPoolExecutor，30s 超时降级）
3. 格式化短期记忆 + 拼接检索上下文 → 注入 Prompt 模板
4. LLM 基于完整上下文流式生成回答（SSE 输出）
5. 末尾附上元数据（should_save 等）供前端判断是否弹出保存提示
6. 回答完成后异步存储聊天记忆至 ChromaDB

**短期记忆 + 长期记忆**：
- 短期记忆：前端传递最近 10 条会话消息，直接注入 Prompt 的 `{history}` 占位符
- 长期记忆：历史对话存入 ChromaDB，新提问时通过语义检索召回相关内容
- 两者互补：短期记忆保证对话连续性，长期记忆跨越会话边界

**SM-2 间隔重复算法**：
- quality ≥ 3 且首次：间隔 = 1 天
- quality ≥ 3 且第二次：间隔 = 6 天
- quality ≥ 3 且后续：间隔 = interval × ease_factor（难度因子动态调整，最低 1.3）
- quality < 3：间隔重置为 1 天
- 创建笔记时自动生成初始复习提醒

### 7.4 数据库设计

#### 7.4.1 MySQL 关系数据库设计

系统使用 MySQL 存储结构化业务数据，主要包括以下核心数据表：

**（1）用户表（tb_user）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 用户唯一标识 |
| username | VARCHAR(50) UNIQUE NOT NULL | 用户名 |
| password | VARCHAR(255) NOT NULL | 加密后的密码 |
| email | VARCHAR(100) | 邮箱 |
| avatar | VARCHAR(255) | 头像 URL |
| created_at | DATETIME | 注册时间 |
| updated_at | DATETIME | 更新时间 |

**（2）笔记表（tb_note）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 笔记唯一标识 |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| title | VARCHAR(200) NOT NULL | 笔记标题 |
| content | LONGTEXT NOT NULL | Markdown 正文内容 |
| category_id | BIGINT FK → tb_category.id | 所属分类 |
| chroma_id | VARCHAR(100) | ChromaDB 中对应的向量文档 ID |
| is_deleted | TINYINT DEFAULT 0 | 逻辑删除标记 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

索引：`idx_user_id`、`idx_category_id`、`idx_created_at`，全文索引 `FULLTEXT(title, content)` 用于关键词搜索。

**（3）分类表（tb_category）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 分类唯一标识 |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| name | VARCHAR(50) NOT NULL | 分类名称 |
| parent_id | BIGINT DEFAULT 0 | 父分类 ID，0 表示根分类 |
| sort_order | INT DEFAULT 0 | 排序序号 |
| created_at | DATETIME | 创建时间 |

**（4）标签表（tb_tag）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 标签唯一标识 |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| name | VARCHAR(50) NOT NULL | 标签名称 |
| created_at | DATETIME | 创建时间 |

**（5）笔记-标签关联表（tb_note_tag）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| note_id | BIGINT FK → tb_note.id | 笔记 ID |
| tag_id | BIGINT FK → tb_tag.id | 标签 ID |

**（6）学习计划表（tb_study_plan）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 计划唯一标识 |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| title | VARCHAR(200) NOT NULL | 计划标题 |
| goal | TEXT | 学习目标描述 |
| stages | JSON | 阶段列表（JSON 数组） |
| is_completed | TINYINT DEFAULT 0 | 是否完成 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

**（7）复习记录表（tb_review）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 记录唯一标识 |
| note_id | BIGINT FK → tb_note.id | 关联笔记 ID |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| quality | INT CHECK(0-5) | 记忆质量评分 |
| interval_days | INT | 当前复习间隔（天） |
| ease_factor | DECIMAL(3,2) | SM-2 难度因子 |
| repetitions | INT | 累计复习次数 |
| next_review_date | DATE | 下次复习日期 |
| reviewed_at | DATETIME | 本次复习时间 |

**（8）学习打卡表（tb_checkin）**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT FK → tb_user.id | 所属用户 |
| checkin_date | DATE NOT NULL | 打卡日期 |
| duration_minutes | INT | 学习时长（分钟） |
| note_count | INT | 当日笔记数 |
| created_at | DATETIME | 创建时间 |

#### 7.4.2 ChromaDB 向量数据库设计

ChromaDB 用于存储文本的向量嵌入，系统创建以下两个 Collection：

**（1）笔记向量库（collection: `notes_{user_id}`）**

| 元数据字段 | 类型 | 说明 |
|-----------|------|------|
| note_id | INT | 对应 MySQL 中笔记 ID |
| title | STRING | 笔记标题 |
| category | STRING | 分类名称 |
| tags | STRING | 标签（逗号分隔） |
| created_at | STRING | 创建时间 |

每条笔记在写入 MySQL 后，异步调用 Sentence Transformers 生成 384 维向量嵌入，并同步存储至 ChromaDB。采用按用户分 Collection 的策略实现多租户隔离。

**（2）聊天记忆向量库（collection: `chat_memory_{user_id}`）**

| 元数据字段 | 类型 | 说明 |
|-----------|------|------|
| role | STRING | 角色（user/assistant） |
| timestamp | STRING | 消息时间戳 |
| session_id | STRING | 会话 ID |

每次对话完成后，异步将用户消息和 AI 回答分别存入聊天记忆向量库，用于长期记忆的语义检索。

### 7.5 API 接口设计

系统 API 遵循 RESTful 设计规范，按功能模块划分如下：

#### 7.5.1 用户认证模块（/api/auth）

| 方法 | 路径 | 说明 | JWT |
|------|------|------|-----|
| POST | /api/auth/register | 用户注册 | × |
| POST | /api/auth/login | 用户登录，返回 JWT Token | × |
| GET | /api/auth/profile | 获取当前用户信息 | √ |

#### 7.5.2 笔记管理模块（/api/notes）

| 方法 | 路径 | 说明 | JWT |
|------|------|------|-----|
| GET | /api/notes | 获取笔记列表（分页） | √ |
| GET | /api/notes/{id} | 获取笔记详情 | √ |
| POST | /api/notes | 创建笔记（含向量同步） | √ |
| PUT | /api/notes/{id} | 更新笔记（含向量更新） | √ |
| DELETE | /api/notes/{id} | 删除笔记（逻辑删除 + 向量删除） | √ |
| GET | /api/notes/search?keyword= | 关键词搜索 | √ |

#### 7.5.3 AI Agent 代理模块（/api/agent）

此模块由 Spring Boot 代理转发至 AI Agent（FastAPI），Spring Boot 仅做认证校验和请求转发：

| 方法 | 路径 | 说明 | 流式 |
|------|------|------|------|
| POST | /api/agent/chat | RAG 智能问答（SSE 流式） | √ |
| POST | /api/agent/chat/sync | RAG 智能问答（非流式） | × |
| POST | /api/agent/semantic-search | 语义搜索 | × |
| POST | /api/agent/extract-graph | 知识图谱提取 | × |
| POST | /api/agent/generate-plan | 学习计划生成 | × |
| POST | /api/agent/summarize | 文本摘要 | × |
| POST | /api/agent/assess-difficulty | 难度评估 | × |
| POST | /api/agent/recommend-tags | 智能标签推荐 | × |
| POST | /api/agent/learning-diagnosis | 学情诊断 | × |

#### 7.5.4 AI Agent 原生接口（/api/v1）

AI Agent（FastAPI）提供的原生 API，支持前后端直接调用（SSE 流式接口）或通过 Spring Boot 代理调用：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/chat/stream | 流式 RAG 问答（SSE） |
| POST | /api/v1/chat | 非流式 RAG 问答 |
| POST | /api/v1/notes/search | 语义搜索笔记 |
| POST | /api/v1/notes/sync | 同步笔记向量 |
| DELETE | /api/v1/notes/{note_id} | 删除笔记向量 |
| POST | /api/v1/knowledge-graph/extract | 提取知识图谱 |
| POST | /api/v1/study-plan/generate | 生成学习计划 |
| POST | /api/v1/smart/summarize | 文本摘要 |
| POST | /api/v1/smart/assess-difficulty | 难度评估 |
| POST | /api/v1/smart/recommend-tags | 标签推荐 |
| POST | /api/v1/smart/learning-diagnosis | 学情诊断 |

#### 7.5.5 分类与标签模块（/api/categories, /api/tags）

| 方法 | 路径 | 说明 | JWT |
|------|------|------|-----|
| GET/POST/PUT/DELETE | /api/categories | 分类 CRUD | √ |
| GET/POST/PUT/DELETE | /api/tags | 标签 CRUD | √ |

#### 7.5.6 学习计划与复习模块（/api/plans, /api/reviews）

| 方法 | 路径 | 说明 | JWT |
|------|------|------|-----|
| GET/POST/PUT/DELETE | /api/plans | 学习计划 CRUD | √ |
| GET | /api/reviews | 获取复习列表 | √ |
| POST | /api/reviews/{id}/checkin | 提交复习评分 | √ |
| GET/POST | /api/checkins | 学习打卡 | √ |

#### 7.5.7 数据统计模块（/api/stats）

| 方法 | 路径 | 说明 | JWT |
|------|------|------|-----|
| GET | /api/stats/overview | 学习仪表盘概览数据 | √ |
| GET | /api/stats/trends | 学习趋势数据 | √ |
| GET | /api/stats/knowledge-distribution | 知识分布数据 | √ |

### 7.6 前端组件架构

前端采用 Vue 3 + TypeScript + Composition API 构建，组件树设计如下：

```
App.vue
├── AuthView.vue                       # 登录/注册页
│   ├── LoginForm.vue                  # 登录表单
│   └── RegisterForm.vue               # 注册表单
├── MainLayout.vue                     # 主布局（侧边栏 + 内容区）
│   ├── Sidebar.vue                    # 侧边导航栏
│   └── <router-view>
│       ├── NoteListView.vue           # 笔记列表页
│       │   ├── NoteFilter.vue         # 笔记筛选器（分类/标签/搜索）
│       │   ├── NoteCard.vue           # 笔记卡片
│       │   └── Pagination.vue         # 分页组件
│       ├── NoteEditorView.vue         # 笔记编辑页
│       │   ├── VditorEditor.vue       # Markdown 编辑器
│       │   ├── TagSelector.vue        # 标签选择器
│       │   └── AiToolbar.vue          # AI 辅助工具栏（摘要/标签推荐等）
│       ├── AiChatView.vue             # AI 助手页
│       │   ├── ChatMessageList.vue    # 消息列表
│       │   │   ├── ChatMessage.vue    # 单条消息
│       │   │   └── ThinkingProcess.vue # 思考过程可视化
│       │   ├── ChatInput.vue          # 输入框（支持 Shift+Enter）
│       │   └── SessionList.vue        # 会话列表
│       ├── KnowledgeGraphView.vue     # 知识图谱页
│       │   ├── GraphCanvas.vue        # ECharts 图谱画布
│       │   └── NodeDetail.vue         # 节点详情面板
│       ├── StudyPlanView.vue          # 学习计划页
│       │   ├── PlanStages.vue         # 阶段列表
│       │   └── PlanTask.vue           # 任务项
│       ├── ReviewView.vue             # 复习管理页
│       │   ├── ReviewList.vue         # 复习列表
│       │   └── ReviewRating.vue       # 记忆质量评分
│       └── DashboardView.vue          # 学习仪表盘页
│           ├── StatCards.vue          # 统计卡片
│           ├── TrendChart.vue         # 趋势折线图
│           ├── DistributionChart.vue  # 知识分布图
│           └── AiDiagnosis.vue        # AI 学情诊断
```

**目录结构**：

```
src/
├── api/                    # 接口层
│   ├── request.ts          # Axios 实例（拦截器、JWT 注入）
│   ├── notes.ts            # 笔记相关 API
│   ├── agent.ts            # AI Agent API（含 SSE fetch 封装）
│   ├── auth.ts             # 认证相关 API
│   ├── plans.ts            # 学习计划 API
│   └── stats.ts            # 数据统计 API
├── stores/                 # Pinia 状态管理
│   ├── user.ts             # 用户状态
│   ├── notes.ts            # 笔记状态
│   ├── chat.ts             # 聊天状态
│   └── settings.ts         # 全局设置
├── composables/            # 组合式函数
│   ├── useSSE.ts           # SSE 流接收 Hook
│   ├── useMarkdown.ts      # Markdown 渲染 Hook
│   └── useScroll.ts        # 智能滚动 Hook
├── components/             # 公共组件
└── views/                  # 页面组件
```

### 7.7 核心功能详细实现

#### 7.7.1 RAG 智能问答实现

**整体流程**：

```
用户提问
    │
    ▼
┌──────────────────────┐
│ 前端构建短期记忆      │  ← 取当前会话最近 10 条消息
│ 封装请求体发送        │
└──────┬───────────────┘
       │ POST /api/agent/chat (SSE)
       ▼
┌──────────────────────┐
│ Spring Boot 代理层    │  ← JWT 鉴权，注入 user_id
│ (WebClient 转发)     │  ← 设置 responseType=text/event-stream
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│ AI Agent (FastAPI + LangChain)            │
│                                            │
│  ① 并行检索（ThreadPoolExecutor）          │
│     ├── 笔记向量库检索                      │
│     │   · embedding(question)              │
│     │   · collection.query(n_results=k)    │
│     │   · 按 user_id 过滤                  │
│     └── 聊天记忆向量库检索                   │
│         · embedding(question)              │
│         · collection.query(n_results=m)    │
│         · 按 user_id 过滤                  │
│     超时 30s 自动降级                       │
│                                            │
│  ② 上下文构造                               │
│     · 短期记忆：{history} ← 最近对话         │
│     · 长期记忆：{long_term_memory} ← 聊天检索│
│     · 笔记上下文：{context} ← 笔记检索结果   │
│                                            │
│  ③ Prompt 模板注入                          │
│     · system: 角色设定 + 行为约束            │
│     · 注入 history + long_term + context    │
│     · 注入 user question                    │
│                                            │
│  ④ LLM 流式生成（DeepSeek API）              │
│     · temperature=0.7                      │
│     · stream=True                          │
│     · 通过 SSE 逐 token 输出                │
│                                            │
│  ⑤ 追问判断                                 │
│     · 问题模糊时先反问澄清                    │
│     · 上下文不足时如实声明                    │
│                                            │
│  ⑥ 异步存储聊天记忆至 ChromaDB              │
└──────────────────────────────────────────┘
       │ SSE (text/event-stream)
       ▼
┌──────────────────────┐
│ 前端 SSE 消费         │
│ ① EventSource / fetch │
│ ② requestAnimationFrame│
│    批量渲染            │
│ ③ marked() 渲染 MD   │
│ ④ 智能滚动跟底        │
└──────────────────────┘
```

**Prompt 模板设计**：

```
System: 你是一个智能知识管理助手。你有权访问用户的个人笔记库来回答问题。
请遵循以下规则：
1. 优先基于提供的笔记上下文回答
2. 如果上下文不足以回答，请如实告知用户
3. 如果用户问题模糊不清，请先反问澄清
4. 回答末尾附上引用的笔记来源
5. 在回答的结尾一行输出 JSON 元数据：
   {"should_save": true/false, "tags": [...]}
   should_save 为 true 表示该回答对用户有价值，建议保存为笔记

短期记忆（当前会话历史）：
{history}

长期记忆（历史相关对话）：
{long_term_memory}

相关知识库片段：
{context}

用户问题：{question}
```

**SSE 事件格式**：
```
data: {"type": "thinking", "phase": "retrieving", "content": "正在检索知识库..."}

data: {"type": "thinking", "phase": "generating", "content": "正在生成回答..."}

data: {"type": "token", "content": "根据"}

data: {"type": "token", "content": "你的"}

data: {"type": "token", "content": "笔记..."}

data: {"type": "done", "metadata": {"should_save": true, "tags": ["算法", "排序"]}}
```

#### 7.7.2 知识图谱提取实现

**流程**：

```
笔记内容
    │
    ▼
┌─────────────────────────┐
│ 文本预处理               │
│ · 分段切片（按标题/段落）│
│ · 过滤过短片段           │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ LLM 概念关系提取          │
│ · Prompt 要求严格 JSON   │
│ · 提取概念节点 + 关系边   │
│ · 标注关系类型和权重      │
└──────┬──────────────────┘
       │ 返回 JSON
       ▼
┌─────────────────────────┐
│ 结果解析与校验            │
│ · 异常 JSON 兜底解析      │
│ · 去重合并已有图谱数据    │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ 前端 ECharts 可视化       │
│ · 力导向图布局           │
│ · 节点大小 ← 关联度       │
│ · 边样式 ← 关系类型       │
│ · 支持拖拽、缩放、点击    │
└─────────────────────────┘
```

**LLM 输出 JSON 格式**：
```json
{
  "concepts": [
    {"name": "快速排序", "category": "算法"},
    {"name": "分治思想", "category": "算法思想"}
  ],
  "relationships": [
    {
      "source": "快速排序",
      "target": "分治思想",
      "type": "前置",
      "weight": 0.9
    }
  ]
}
```

**关系类型定义**：
- **前置（prerequisite）**：A 是学习 B 的前置知识
- **扩展（extends）**：B 是 A 的扩展和深化
- **相关（related）**：A 与 B 存在内容关联
- **矛盾（contradicts）**：A 与 B 存在观点或结论不一致

#### 7.7.3 学习计划生成实现

用户输入学习目标（如"三个月内掌握机器学习基础"），系统调用 LLM 生成结构化学习路径：

**请求体**：
```json
{
  "goal": "三个月内掌握机器学习基础",
  "user_id": 1,
  "available_hours_per_week": 10,
  "existing_knowledge": ["Python 基础", "高等数学"],
  "note_ids": [1, 2, 3]
}
```

**LLM 输出 JSON 格式**：
```json
{
  "title": "机器学习基础学习计划",
  "stages": [
    {
      "name": "第一阶段：数学基础复习",
      "description": "复习机器学习所需的线性代数和概率论核心概念",
      "topics": ["矩阵运算", "特征值与特征向量", "概率分布", "贝叶斯公式"],
      "estimated_days": 14,
      "resources": ["3Blue1Brown 线性代数视频", "《概率论与数理统计》"]
    },
    {
      "name": "第二阶段：监督学习入门",
      "description": "学习常见的监督学习算法及其应用",
      "topics": ["线性回归", "逻辑回归", "决策树", "随机森林"],
      "estimated_days": 21,
      "resources": ["吴恩达 Machine Learning 课程", "sklearn 官方文档"]
    }
  ]
}
```

前端将 stages 渲染为阶段列表，每阶段下展示主题列表，用户可勾选完成状态。进度 = 已完成主题数 / 总主题数。

#### 7.7.4 SM-2 间隔重复实现

**算法核心逻辑**：

```
输入：用户对笔记记忆质量的评分 quality (0-5)
     当前难度因子 ease_factor
     当前累计复习次数 repetitions
     当前复习间隔 interval_days

步骤：
1. IF quality >= 3:
     IF repetitions == 0:
         interval = 1 (天)
     ELSE IF repetitions == 1:
         interval = 6 (天)
     ELSE:
         interval = round(interval_days * ease_factor)
     repetitions += 1
   ELSE:
     repetitions = 0
     interval = 1 (天)

2. ease_factor = ease_factor + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02))
   ease_factor = max(1.3, ease_factor)

3. next_review_date = today + interval

输出：更新后的 ease_factor, repetitions, interval, next_review_date
```

**复习流程**：

```
创建笔记 → 自动生成初始复习提醒（次日）
    │
    ▼
到达复习日期 → 前端提醒 → 用户阅读笔记
    │
    ▼
用户自评记忆质量 (0-5)
    │
    ▼
SM-2 算法计算下次复习日期 → 更新 tb_review 表 → 等待下次复习
```

**前端评分界面**：用户阅读完笔记后，选择记忆质量等级（0=完全忘记，3=基本记得，5=完全掌握），系统自动计算下次复习日期。

#### 7.7.5 双通道记忆实现

**短期记忆（会话级）**：

前端维护当前会话的消息数组 `sessionMessages[]`，每次发送提问时，取最近 10 条格式化后放入请求体的 `history` 字段：

```
User: 什么是快速排序？
Assistant: 快速排序是一种基于分治思想的排序算法...
User: 它的时间复杂度是多少？
```

注入 Prompt 的 `{history}` 占位符，使 LLM 感知当前会话上下文。

**长期记忆（跨会话级）**：

```
对话完成后
    │
    ▼
┌──────────────────────────┐
│ 异步存储至 ChromaDB       │
│ collection: chat_memory_{user_id}
│ · 用户消息 → role=user   │
│ · AI 回答 → role=assistant│
│ · 均含 session_id 关联    │
└──────────────────────────┘

下次提问时
    │
    ▼
┌──────────────────────────┐
│ 向量检索相关历史对话       │
│ · embedding(question)     │
│ · 按 user_id 过滤         │
│ · top-k=3 召回            │
│ · 注入 long_term_memory   │
└──────────────────────────┘
```

**协同机制**：
- 短期记忆保证同一会话内的指代理解和对话连贯性
- 长期记忆跨越会话边界，让 AI "记住"历史学习内容
- 两者互补，短期记忆（精确、近时间）、长期记忆（语义、广范围）

#### 7.7.6 流式输出与渲染实现

**Spring Boot WebClient SSE 代理**：

```java
// WebClient 配置（响应式 SSE 流代理）
@PostMapping(value = "/chat", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public Flux<ServerSentEvent<String>> proxyChat(@RequestBody ChatRequest request) {
    Long userId = JwtUtil.getCurrentUserId();
    request.setUserId(userId);

    return webClient.post()
            .uri(aiAgentUrl + "/api/v1/chat/stream")
            .bodyValue(request)
            .accept(MediaType.TEXT_EVENT_STREAM)
            .retrieve()
            .bodyToFlux(new ParameterizedTypeReference<ServerSentEvent<String>>() {})
            .timeout(Duration.ofSeconds(120));
}
```

**前端 SSE 消费 + requestAnimationFrame 批量渲染**：

```typescript
// composables/useSSE.ts 核心逻辑
function useSSE() {
  const tokenBuffer: string[] = []        // token 缓冲区
  let rafId: number | null = null         // rAF 句柄

  async function startStream(url: string, body: object) {
    const response = await fetch(url, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
      body: JSON.stringify(body)
    })

    const reader = response.body!.getReader()
    const decoder = new TextDecoder()

    while (true) {
      const { done, value } = await reader.read()
      if (done) break

      const text = decoder.decode(value)
      const lines = text.split('\n')

      for (const line of lines) {
        if (line.startsWith('data: ')) {
          const data = JSON.parse(line.slice(6))

          if (data.type === 'token') {
            // 将 token 加入缓冲区，等待 rAF 统一渲染
            tokenBuffer.push(data.content)
            scheduleRender()
          } else if (data.type === 'done') {
            // 最终渲染 + 处理元数据
            flushBuffer()
            handleMetadata(data.metadata)
          }
        }
      }
    }
  }

  function scheduleRender() {
    if (rafId !== null) return  // 已有待渲染帧
    rafId = requestAnimationFrame(() => {
      // 同一帧内到达的所有 token 一次性拼接渲染
      const fullText = tokenBuffer.join('')
      renderedMarkdown.value = marked(fullText)
      tokenBuffer.length = 0
      rafId = null
    })
  }
}
```

**思考过程可视化**：

前端根据 SSE 事件中的 `phase` 字段，分阶段展示 AI 处理状态：

```
阶段 1：「🔍 检索知识库中...」  ← phase: "retrieving"
阶段 2：「✍️ 正在生成回答...」  ← phase: "generating"
阶段 3： 逐字显示回答内容       ← type: "token"
```

**停止生成与重新生成**：
- **停止生成**：前端调用 `reader.cancel()` 中断 SSE 流，Spring Boot 层面通过 `Flux.cancel()` 链路传递取消信号
- **重新生成**：保留原提问和上下文，重新发起请求（`temperature` 可增加微小扰动以获得不同回答）

#### 7.7.7 智能辅助功能实现

**（1）文本摘要**

用户选中笔记内容，调用 LLM 生成中文摘要。Prompt 设计：

```
请对以下文本生成简洁的中文要点摘要，使用无序列表格式，不超过 5 条：
{text}
```

**（2）难度评估**

评估学习材料的难度等级。Prompt 设计：

```
请评估以下学习材料的难度，仅回复以下三个等级之一：
- "beginner"（初学者）
- "intermediate"（中级）
- "advanced"（高级）

材料内容：
{text}
```

**（3）智能标签推荐**

根据笔记内容自动推荐分类和标签。Prompt 设计：

```
根据以下笔记内容，推荐合适的分类和标签。
请以 JSON 格式输出：
{"category": "推荐分类", "tags": ["标签1", "标签2", "标签3"]}

笔记内容：
{text}
```

**（4）学情诊断**

聚合用户学习数据（笔记数、学习时长、知识分布、复习完成率等），由 AI 分析给出个性化建议。Prompt 设计：

```
你是一位学习诊断专家。请根据以下用户的学习数据，分析其学习状况并给出个性化建议：

- 总笔记数：{total_notes}
- 本周学习时长：{weekly_duration} 分钟
- 知识分布：{knowledge_distribution}
- 复习完成率：{review_completion_rate}%
- 连续打卡天数：{streak_days}

请从学习习惯、知识结构、薄弱环节三个维度进行分析，并给出 3 条具体可行的改进建议。
```

### 7.8 Docker Compose 部署架构

系统使用 Docker Compose 实现一键部署，各服务通过 Docker 网络 `knowledge-network` 互联：

```
┌─────────────────────────────────────────────────┐
│              Docker Compose 服务编排              │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ frontend │  │ backend  │  │   ai-agent    │  │
│  │ (nginx)  │  │ (Spring  │  │  (FastAPI +   │  │
│  │  :80     │  │  Boot)   │  │   LangChain)  │  │
│  │          │  │  :8080   │  │    :8000      │  │
│  └────┬─────┘  └────┬─────┘  └──────┬────────┘  │
│       │             │               │            │
│       │    proxy_pass /api/ → backend:8080       │
│       │             │               │            │
│       │             └──转发 AI 请求──┘            │
│       │                            │              │
│  ┌────┴───────┐            ┌──────┴────────┐   │
│  │   MySQL    │            │   ChromaDB    │   │
│  │   :3306    │            │    :8001      │   │
│  └────────────┘            └───────────────┘   │
│                                                  │
│  network: knowledge-network                      │
└─────────────────────────────────────────────────┘
```

**docker-compose.yml 核心配置**：

```yaml
version: '3.8'
services:
  frontend:
    build: ./frontend
    ports: ["80:80"]
    depends_on: [backend]

  backend:
    build: ./backend
    ports: ["8080:8080"]
    environment:
      - DB_HOST=mysql
      - AI_AGENT_URL=http://ai-agent:8000
    depends_on: [mysql]

  ai-agent:
    build: ./ai-agent
    ports: ["8000:8000"]
    environment:
      - CHROMA_HOST=chromadb
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    depends_on: [chromadb]

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=knowledge_assistant
    volumes: [mysql_data:/var/lib/mysql]

  chromadb:
    image: chromadb/chroma:latest
    volumes: [chroma_data:/chroma/chroma]

volumes:
  mysql_data:
  chroma_data:
```

---

## 八、创新点与特色

1. **完整的学习闭环**：将笔记记录、AI 问答、知识图谱、学习规划、间隔复习、智能标签推荐、学情诊断串联成完整的学习流程

2. **双通道记忆机制**：短期记忆（会话历史 Prompt 注入）+ 长期记忆（向量化存储语义检索），使 AI 既知道"刚才聊了什么"也能回忆起"以前学过什么"

3. **流式输出体验优化**：requestAnimationFrame 批量渲染消除界面抖动，思考过程分阶段可视化，支持停止生成与重新生成

4. **多路并行 RAG 检索**：同时检索笔记向量和聊天记忆，超时自动降级，保证可用性

5. **本地化向量存储**：ChromaDB 本地持久化 + Sentence Transformers 本地嵌入，无需向外部服务上传用户数据，保护用户隐私

6. **认知科学与 AI 的结合**：SM-2 间隔重复算法与 LLM 生成能力的协同，既保证了复习调度的科学性，又提供了灵活的内容生成

7. **微服务化 AI Agent**：AI Agent 独立部署，Spring Boot 作为代理层，便于独立扩展、升级或替换模型

---

## 九、开发计划与进度安排

| 阶段 | 时间 | 主要任务 |
|------|------|---------|
| 第一阶段：需求分析与技术调研 | 第 1-2 周 | 分析现有知识管理工具的不足；调研 RAG、LangChain、ChromaDB 等技术方案；确定技术栈和系统架构 |
| 第二阶段：系统设计 | 第 3-4 周 | 完成数据库设计（MySQL + ChromaDB）；完成前端页面设计和 API 接口设计；编写模块设计文档 |
| 第三阶段：核心功能开发 | 第 5-10 周 | Spring Boot 业务后端开发；AI Agent 服务开发（RAG 问答、知识图谱、学习计划、嵌入同步、标签推荐、学情诊断）；前端页面开发（笔记管理、AI 助手、知识图谱、仪表盘） |
| 第四阶段：前后端联调 | 第 11-12 周 | 前后端接口对接；流式问答联调；短期记忆和长期记忆验证；思考过程可视化调试 |
| 第五阶段：测试与优化 | 第 13-14 周 | 功能测试、性能测试、异常场景测试；优化流式渲染流畅性和检索质量；修复 Bug |
| 第六阶段：文档撰写与答辩准备 | 第 15-16 周 | 撰写毕业设计说明书；制作答辩 PPT；系统部署和演示准备 |

---

## 十、预期成果

1. **可运行的系统原型**：完整的前后端代码，支持用户注册登录、笔记管理、AI 流式问答（含短期/长期记忆）、知识图谱浏览、学习计划管理、复习提醒、智能标签推荐、学情诊断等功能

2. **毕业设计说明书**：详细阐述系统架构、模块设计、关键实现、数据流链路和测试结果

3. **源代码与部署方案**：完整的源代码，Docker Compose 一键部署脚本

4. **系统演示视频**：展示系统核心功能的使用流程

---

## 十一、应具备的基本条件

### 1. 理论基础条件

具备 Java 程序设计、Spring Boot 框架、Vue 前端框架、数据库原理、软件工程等相关专业基础知识；了解大语言模型（LLM）的基本原理、检索增强生成（RAG）的技术流程、向量数据库的语义检索机制以及 SM-2 间隔重复算法的基本逻辑；能够理解前后端分离架构的设计原则、SSE 流式传输协议以及 RESTful API 的规范设计方法。

### 2. 技术实践条件

掌握 Spring Boot + MyBatis-Plus 后端开发技术，熟悉 MySQL 数据库建表、索引、多表关联查询及事务操作；掌握 Vue 3 + TypeScript + Element Plus 前端开发，能够使用 Pinia 进行状态管理、Vditor 进行 Markdown 编辑、ECharts 进行图表渲染；掌握 FastAPI + LangChain 框架的基本使用，能够调用 DeepSeek API 完成 Prompt 构建与 LLM 交互；熟悉 ChromaDB 向量数据库的 collection 管理与 embedding 检索操作；具备基本的 Docker Compose 容器化部署与 Git 版本管理能力，能够独立完成项目编码、调试与 BUG 修复。

### 3. 软硬件条件

具备正常开发使用的计算机设备（Windows 11 操作系统），拥有 JDK 17+、IntelliJ IDEA、PyCharm / VS Code、MySQL 8.0、Navicat、Git、Docker Desktop 等开发环境；拥有 DeepSeek API 可用额度用于 LLM 调用；能够完成项目搭建、本地运行、前后端联调与部署测试。

### 4. 学习与研究条件

具备查阅国内外文献、跟踪 RAG 与 AI Agent 技术前沿、参考 LangChain 等开源项目文档的能力；能够独立分析知识管理领域的业务需求，定位并解决开发过程中的技术问题（如检索质量优化、LLM 幻觉控制、流式渲染流畅性等）；能够按照毕业设计进度安排按时完成需求分析、系统设计、编码开发、测试优化、论文撰写与答辩工作。

---

## 十二、可行性分析

### 10.1 技术可行性

- DeepSeek API 提供成熟的大语言模型服务，API 与 OpenAI 兼容，按月计费，成本可控
- LangChain 和 FastAPI 有完善的文档和社区支持
- ChromaDB 和 Sentence Transformers 均为开源项目，可本地部署，无授权费用
- Vue 3、Spring Boot 等框架均为广泛使用的成熟技术
- Spring WebClient 支持响应式 SSE 流代理，天然适配流式问答场景

### 10.2 时间可行性

- 计划共 16 周，各阶段时间分配合理
- 核心开发阶段（第 5-10 周）共 6 周，按模块化开发并行推进，时间充足
- 预留 4 周用于测试、联调和文档撰写

### 10.3 资源可行性

- 开发工具均为免费/开源（VS Code、IntelliJ IDEA Community、Git）
- DeepSeek API 按量付费，开发测试阶段费用极低
- 本地运行 ChromaDB + Sentence Transformers，无需额外服务器或 API 费用
- 个人计算机即可满足开发、测试和部署需求

---

## 十三、参考文献

[1] Vaswani A, Shazeer N, Parmar N, et al. Attention is all you need[C]. Advances in Neural Information Processing Systems, 2017.

[2] Lewis P, Perez E, Piktus A, et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks[C]. Advances in Neural Information Processing Systems, 2020.

[3] Wozniak P A, Gorzelanczyk E J. Optimization of repetition spacing in the practice of learning[J]. Acta Neurobiologiae Experimentalis, 1994, 54(1): 57-62.

[4] LangChain. LangChain Documentation[EB/OL]. https://python.langchain.com/, 2024.

[5] Chroma. ChromaDB Documentation[EB/OL]. https://docs.trychroma.com/, 2024.

[6] Reimers N, Gurevych I. Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks[C]. Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing, 2019.

[7] DeepSeek. DeepSeek API Documentation[EB/OL]. https://platform.deepseek.com/, 2024.

[8] 尤雨溪. Vue 3 官方文档[EB/OL]. https://vuejs.org/, 2024.

[9] 陈昊. 基于知识图谱的个性化学习路径推荐系统研究[D]. 北京邮电大学, 2023.
